from django.urls import path

from rest_framework_simplejwt.views import (
    TokenRefreshView,
)


urlpatterns = [
    #path('product/', MyTokenObtainPairView.as_view()),
    #path('order/', TokenRefreshView.as_view()),
]
